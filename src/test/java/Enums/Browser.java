package Enums;

public enum Browser {

    CHROME("chrome"),
    FIREFOX("firefox"),
    EDGE("edge");

    String browserName;

    Browser(String browserName) { this.browserName = browserName; }

    }

